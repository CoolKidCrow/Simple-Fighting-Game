public class Enums {
    public enum Scenes {
        title, help, credits, game, characterSelect
    }

    public enum Inputs {
        punch, kick, right, left, block, jump, crouch
    }

    public enum AnimType {
        standing, crouching, jumping
    }

    public enum BoxType {
        highHit, lowHit, overheadHit, hurtBox
    }

    public enum character {
        ryu, ken
    }

    public enum map {
        usa, japan
    }
}
